# RentorHire

**RentorHire** is a platform that allows users and service providers to rent or hire various items conveniently. This platform bridges the gap between those looking to rent items and those offering items for rent. Whether it's for a short-term need or a long-term rental, **RentorHire** makes it easier to find and offer rental services.

---

### Setup Instructions:

1. **Clone the repository:**

   ```bash
   git clone https://github.com/lavaniapankaj/rentorhire.git
   ```

2. **Install dependencies for the main folder:**
   Navigate to the project directory and install the required dependencies:

   ```bash
   cd rentorhire
   npm install
   ```

3. **Install dependencies for the backend:**
   Navigate to the `backend` folder and install the backend dependencies:

   ```bash
   cd rentorhire/backend
   npm install
   ```

4. **Install dependencies for the frontend:**
   Navigate to the `frontend` folder and install the frontend dependencies:

   ```bash
   cd ../frontend
   npm install
   ```

5. **Authentication Setup:**
   For authentication, install the required packages:

   ```bash
   npm install jwt-simple bcryptjs
   ```

6. **Media Upload:**
   For Media upload, install the required packages:

   ```bash
   npm install multer
   ```

7. **Bootstrap:**
   For Bootstrap, install the required packages:

   ```bash
   npm install bootstrap
   ```

8. **Email Sender:**
   For Email Sender, install the required packages:

   ```bash
   npm install nodemailer
   ```
9. **Extra Library:**
   For Extra Library, install the required packages:

   ```bash
   npm install concurrently --save-dev
   ```
10. **SendGrid Mailer Library:**
      For Sendgrid Email notification, install the required packages:

      ```bash
      npm install @sendgrid/mail
      ```

11. **React Jodit Library:**
      For React Jodit WYSIWYG, install the required packages: (New packages for before live) and remove old packages (npm uninstall quill react-quilljs and npm uninstall react-quill)

      ```bash
      npm install jodit-react jodit   ( https://xdsoft.net/jodit/ )
      ```
12. **React Icons Library:**
      For React Icons, install the required packages:

      ```bash
      npm install react-icons --save  (https://react-icons.github.io/react-icons/icons/lu/)
      ```
13. **MYSQL 2:**
      For MySQL, install the required packages:

      ```bash
      npm install --save mysql2
      ```
14. **SVG Webpack:**
      For SVG Webpack, install the required packages:

      ```bash
      npm install --save-dev @svgr/webpack
      ```
15. **CSV:**
      For CSV, install the required packages:

      ```bash
      npm install csv-parser
      ```
16. **Geo IP Lite:**
      For Geo Ip Lite, install the required packages:

      ```bash
      npm install geoip-lite
      ```
17. **Cron:**
      For CRON, install the required packages:

      ```bash
      npm install node-cron
      ```
      * * * * *
      │ │ │ │ │
      │ │ │ │ └─ Weekday
      │ │ │ └── Month
      │ │ └─── Day
      │ └──── Hour
      └───── Minute
      
18. **The carousel package**
      To use the carousels in the frontend
      ````
      npm install embla-carousel-react
      ````

19. **In Future:**      